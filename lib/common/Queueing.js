'use strict';

const { unimplemented } = require('./utils');

class Queueing {
    add() {
        unimplemented();
    }

    addAgain() {
        unimplemented();
    }

    next() {
        unimplemented();
    }

    /**
     * @return {number}
     */
    getNumberWaiting() {
        unimplemented();
    }
}

module.exports = Queueing;
